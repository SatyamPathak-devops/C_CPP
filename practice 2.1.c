//calculate side of a square

#include <stdlib.h>

int main()
{
    float side;
    printf("Give value of side in order to calculate the area of the square: ");
    scanf("%f",&side);
    printf("The area of square is: %f",side*side);
    
    return 0;
    
}